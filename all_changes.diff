diff --git a/light/light.h b/light/light.h
index e47d4dd9..5691cd08 100644
--- a/light/light.h
+++ b/light/light.h
@@ -435,6 +435,8 @@ qboolean BoxInSolid(vec3_t origin, float margin, qboolean structuralonly);
 
 void TraceLine(const vec3_t start, const vec3_t stop, trace_t *trace,
 			   qboolean testAll, traceWork_t *tw);
+
+qboolean GatherGridAmbientBleed(vec3_t origin, vec3_t envColor, vec3_t bentAccum, traceWork_t *tw);
 qboolean PointInSolid(vec3_t start);
 qboolean TriSoupSamplePoint(dsurface_t *ds, float st[2], vec3_t origin, vec3_t normal, vec3_t outCentroid);
 qboolean PatchSamplePoint(mesh_t *mesh, float st[2], vec3_t origin, vec3_t normal, vec3_t outCentroid);
diff --git a/light/light_ambient.c b/light/light_ambient.c
index 22f76ad4..8aa2888a 100644
--- a/light/light_ambient.c
+++ b/light/light_ambient.c
@@ -320,6 +320,92 @@ static qboolean GatherAmbientAtPoint(vec3_t origin, vec3_t normal, vec3_t ambCol
     return qfalse;
 }
 
+/*
+================
+GatherGridAmbientBleed
+================
+*/
+qboolean GatherGridAmbientBleed(vec3_t origin, vec3_t envColor, vec3_t bentAccum, traceWork_t *tw)
+{
+    float gatherRadius = ambient_gatheradius;
+    float gatherRadiusSq = gatherRadius * gatherRadius;
+
+    int ix_min = (int)((origin[0] - gatherRadius - gridMins[0]) / gridSize[0]);
+    int ix_max = (int)((origin[0] + gatherRadius - gridMins[0]) / gridSize[0]);
+    int iy_min = (int)((origin[1] - gatherRadius - gridMins[1]) / gridSize[1]);
+    int iy_max = (int)((origin[1] + gatherRadius - gridMins[1]) / gridSize[1]);
+    int iz_min = (int)((origin[2] - gatherRadius - gridMins[2]) / gridSize[2]);
+    int iz_max = (int)((origin[2] + gatherRadius - gridMins[2]) / gridSize[2]);
+
+    // clamp to bounds
+    if (ix_min < 0) ix_min = 0; 
+    if (ix_max > gridBounds[0] - 1) ix_max = gridBounds[0] - 1;
+    
+    if (iy_min < 0) iy_min = 0; 
+    if (iy_max > gridBounds[1] - 1) iy_max = gridBounds[1] - 1;
+    
+    if (iz_min < 0) iz_min = 0; 
+    if (iz_max > gridBounds[2] - 1) iz_max = gridBounds[2] - 1;
+
+    VectorClear(envColor);
+    VectorClear(bentAccum);
+    float totalWeight = 0.0f;
+    
+    for (int gz = iz_min; gz <= iz_max; gz++) {
+        for (int gy = iy_min; gy <= iy_max; gy++) {
+            for (int gx = ix_min; gx <= ix_max; gx++) {
+                int gidx = (gz * gridBounds[1] + gy) * gridBounds[0] + gx;
+                float *gCol = &maoAmbient[gidx * 3];
+                
+                float lum = gCol[0] * 0.299f + gCol[1] * 0.587f + gCol[2] * 0.114f;
+                if (lum <= 0.0001f) continue; // Skip solid/black voxels
+
+                vec3_t gPos;
+                gPos[0] = gridMins[0] + gx * gridSize[0];
+                gPos[1] = gridMins[1] + gy * gridSize[1];
+                gPos[2] = gridMins[2] + gz * gridSize[2];
+
+                vec3_t dir;
+                VectorSubtract(gPos, origin, dir);
+                
+                float distSq = DotProduct(dir, dir);
+                if (distSq > gatherRadiusSq) continue;
+                if (distSq < 1.0f) distSq = 1.0f; // Prevent div by zero
+
+                float dist = sqrtf(distSq);
+                VectorScale(dir, 1.0f / dist, dir); // Normalize
+
+                float w = 1.0f - (distSq / gatherRadiusSq);
+
+                // Trace ray
+                trace_t trace;
+                TraceLine(origin, gPos, &trace, qfalse, tw);
+                if (trace.passSolid) {
+                    continue; // Skip unreachable cells entirely, don't account for averaging
+                }
+                
+                envColor[0] += gCol[0] * w;
+                envColor[1] += gCol[1] * w;
+                envColor[2] += gCol[2] * w;
+                
+                bentAccum[0] += dir[0] * w * lum;
+                bentAccum[1] += dir[1] * w * lum;
+                bentAccum[2] += dir[2] * w * lum;
+                
+                totalWeight += w;
+            }
+        }
+    }
+
+    if (totalWeight > 0.0001f) {
+        envColor[0] /= totalWeight;
+        envColor[1] /= totalWeight;
+        envColor[2] /= totalWeight;
+        return qtrue;
+    }
+    return qfalse;
+}
+
 /*
 ================
 TraceAmbient
diff --git a/light/light_direct.c b/light/light_direct.c
index 65d91d24..22e39774 100644
--- a/light/light_direct.c
+++ b/light/light_direct.c
@@ -697,6 +697,7 @@ qboolean SunToPoint(const vec3_t origin, traceWork_t *tw, contribution_t *out,
     out->isGlow = qfalse;
     out->ambientClampScale = 1.0f;
     out->noambient = qfalse;
+    out->familyCount = 1;
 
     return qtrue;
 }
@@ -1098,6 +1099,7 @@ qboolean LightContributionToPoint(const light_t *light, const vec3_t origin,
         if (fCount <= 0) fCount = 1;
         out->ambientClampScale = 1.0f / (float)fCount;
         out->noambient = light->noambient;
+        out->familyCount = fCount;
         return qtrue;
     }
 
@@ -1211,6 +1213,7 @@ qboolean LightContributionToPoint(const light_t *light, const vec3_t origin,
     if (fCount <= 0) fCount = 1;
     out->ambientClampScale = 1.0f / (float)fCount;
     out->noambient = light->noambient;
+    out->familyCount = fCount;
 
     return qtrue;
 }
@@ -2360,8 +2363,8 @@ void TraceLights(int num)
 
 float lightgridMaxDisplayIntensity = 0.0f;
 
-#define LIGHTGRID_AMBIENT_TRANSFER_FRACTION 0.25f
-#define LIGHTGRID_AMBIENT_MAX_LUMA 0.10f
+#define LIGHTGRID_AMBIENT_TRANSFER_FRACTION 0.20f
+#define LIGHTGRID_AMBIENT_MAX_LUMA 0.5f
 
 void TraceGrid(int num)
 {
@@ -2518,9 +2521,28 @@ void TraceGrid(int num)
             vec3_t ambContrib;
             VectorScale(tempColor, LIGHTGRID_AMBIENT_TRANSFER_FRACTION, ambContrib);
             
-            float clampLuma = lightgridMaxDisplayIntensity * LIGHTGRID_AMBIENT_MAX_LUMA * contributions[i].ambientClampScale;
+            float maxCurveLuma = lightgridMaxDisplayIntensity;
             float luma = ambContrib[0] * 0.299f + ambContrib[1] * 0.587f + ambContrib[2] * 0.114f;
             
+            if (0 && luma > 0.0001f && maxCurveLuma > 0.0001f)
+            {
+                // Math Trick: Artificially inflate luminance by the family size.
+                // For standard point lights (familyCount == 1), this changes nothing.
+                // For subdivided surface lights (familyCount > 1), this correctly mimics 
+                // the full surface's intensity before running it through the curve.
+                float simulated_luma = luma * contributions[i].familyCount;
+                
+                float new_simulated_luma = maxCurveLuma * (1.0f - expf(-simulated_luma / maxCurveLuma));
+                
+                // Deflate back to the patch's individual share
+                float new_luma = new_simulated_luma / contributions[i].familyCount;
+                
+                float curveScale = new_luma / luma;
+                VectorScale(ambContrib, curveScale, ambContrib);
+                luma = new_luma;
+            }
+            
+            float clampLuma = lightgridMaxDisplayIntensity * LIGHTGRID_AMBIENT_MAX_LUMA;
             if (luma > clampLuma)
             {
                 float scale = clampLuma / luma;
@@ -2544,13 +2566,33 @@ void TraceGrid(int num)
 
         if (maoAmbient)
         {
-            float dirRatio = 0.45f;
-            float ambRatio = 0.75f;
+            float dirRatio = 0.1f;
+            float ambRatio = 1.0f;
             vec3_t moveDir;
             vec3_t moveAmb;
+            vec3_t envAmb;
+            vec3_t bentAccum;
             
-            VectorScale(&maoAmbient[num * 3], dirRatio, moveDir);
-            VectorScale(&maoAmbient[num * 3], ambRatio, moveAmb);
+            // Start with cell's own base MAO value
+            VectorCopy(&maoAmbient[num * 3], moveAmb);
+            VectorClear(moveDir);
+
+            // Attempt to gather environmental bleed
+            if (GatherGridAmbientBleed(origin, envAmb, bentAccum, tw))
+            {
+                // 60% base, 40% gathered environment
+                moveAmb[0] = moveAmb[0] * 0.60f + envAmb[0] * 0.40f;
+                moveAmb[1] = moveAmb[1] * 0.60f + envAmb[1] * 0.40f;
+                moveAmb[2] = moveAmb[2] * 0.60f + envAmb[2] * 0.40f;
+
+                if (VectorNormalize(bentAccum, moveDir) < 0.0001f)
+                {
+                    VectorClear(moveDir);
+                }
+            }
+
+            VectorScale(moveDir, dirRatio, moveDir);
+            VectorScale(moveAmb, ambRatio, moveAmb);
             
             gridData32[num].ambient[0][0] += moveAmb[0];
             gridData32[num].ambient[0][1] += moveAmb[1];
diff --git a/light/lightdata.h b/light/lightdata.h
index 78073f21..c1e55e78 100644
--- a/light/lightdata.h
+++ b/light/lightdata.h
@@ -11,6 +11,7 @@ typedef struct {
   float ambientClampScale;
   qboolean isGlow;
   qboolean noambient;
+  int familyCount;
 } contribution_t;
 
 // High-precision vertex for internal lighting
diff --git a/makebspdata/qfusion.json b/makebspdata/qfusion.json
deleted file mode 100644
index ff09f00c..00000000
--- a/makebspdata/qfusion.json
+++ /dev/null
@@ -1,66 +0,0 @@
-{
-  "game": "qfusion",
-  "template": "",
-  "rootDir": ".",
-  "userDir": "",
-  "gameDir": "basewf",
-  "bspIdent": "FBSP",
-  "bspVersion": 1,
-  "lumpCount": 18,
-  "maxLMSurfaceVerts": 65535,
-  "maxSurfaceVerts": 65535,
-  "maxSurfaceIndexes": 393210,
-  "lightmapSize": 512,
-  "externalLightmaps": false, /* if true, lightmaps are stored as external .tga files instead of inside the BSP */
-  "keepLights": true, /* if true, light entities are not stripped from the BSP */
-  "sampleSize": 4,
-  "defaultGridSize": [32.00, 32.00, 64.00],
-  "hdr": "rgb8", /* [ off, rgb8, rgb16, rgb32 ] More than 8 bit requires a bsp version change */
-  "hdr8BitScale": 3.00,
-  "lightmapsRGB": true,
-  "lightgridRGB": false,
-  "lightgridAmbientBias": 2.20,
-  "lightgridDirectBias": 2.20,
-  "lightgridSmoothAmbient": 128.00, /* Radius in world units for smoothing the ambient component of the light grid */
-  "lightgridSmoothAmbientPasses": 4,
-  "lightgridMinAmbient": 0.00,
-  "lightgridMaxAmbient": 0.00,
-  "texturesRGB": true,
-  "colorsRGB": true,
-  "radiosityPasses": 4, /* Number of times the light bounces */
-  "radiosityIntensity": 1.00,
-  "radiosityColorRatio": 1.00, /* Percentage (0.0 to 1.0) of surface color transferred to the bounce light */
-  "radiosityInterval": 4, /* Radiosity grid sample size */
-  "rad_ao_intensity": 0.50, /* Intensity of ambient occlusion shadowing */
-  "rad_ao_min": 0.00, /* Minimum distance for ambient occlusion shadowing */
-  "rad_ao_max": 32.00, /* Maximum distance for ambient occlusion shadowing */
-  "ambientTestRadius": 512.00, /* Radius at which a light probe tests for being occluded */
-  "ambientGatherRadius": 256.00, /* Distance at which a lightmap pixel gathers light from probes */
-  "shading": "lambert",  /* [ lambert, halflambert, quadratic, doublequadratic, unreal ] */
-  "sunShading": "lambert",  /* [ lambert, halflambert, quadratic, doublequadratic, unreal ] */
-  "attenuation": "standard",  /* [ standard, soft, linear ] */
-  "cutoff": 0.100000, /* Minimum remaining light energy to apply the contribution to a surface */
-  "fadeout": 0.000000, /* Percentage of the light's outer radius to fade linearly until reaching cutoff */
-  "backSplashSpot": 0.100000, /* Default entity spotlight backsplash fraction (0.0 to 1.0) */
-  "backSplashSurface": 0.000000, /* Default surface light backsplash fraction (0.0 to 1.0) */
-  "deluxeMap": true,
-  "deluxeMinAngle": 15.00,
-  "antialiasingPasses": 0, /*Number of post-process AA passes */
-  "superSampleRadius": 0.00,
-  "smoothPasses": 4, /* passes of blurring lightmaps */
-  "smoothRadius": 0.35, /* fractional values accepted. Minimum 0.1 */
-  "exposurefilter": "reinhard", /* [ off, softknee, reinhard, filmic ] */
-  "saturation": 1.25, /* Multiplier (1.0 = normal, 0.0 = grayscale) */
-  "saturationRamp": "halfpower", /* [ off, filmic, power, midtone ] */
-  "chamferEdges": true,
-  "chamferConvexWidth": 1.25,
-  "chamferConcaveWidth": 0.00,
-  "decalExtrusion": 0.00,
-  "enforceSampleSize": true,
-  "forceUVGen": true,
-  "flareShader": "",
-  "haloShader": "halo",
-  "customSurfaceParms": [
-    { "name": "nowalljump", "clearSolid": false, "surfaceFlags": 4194304, "contentFlags": 0 }
-  ]
-}
