name: Build makebsp

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-gcc make

      - name: Build
        shell: msys2 {0}
        run: |
          make clean
          make RELEASE=1

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: makebsp-windows
          path: |
            makebsp.exe
            makelight.exe
            embree4.dll
            tbb12.dll
            tbbmalloc.dll
            makebspdata/warfork.json
            makebspdata/xonotic.json

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential ocl-icd-opencl-dev

      - name: Build
        run: |
          make clean
          make RELEASE=1

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: makebsp-linux
          path: |
            makebsp.elf
            makelight.elf
            libs/embree/prebuilt/linux/lib/libembree4.so*
            libs/embree/prebuilt/linux/lib/libtbb*.so*
            makebspdata/warfork.json
            makebspdata/xonotic.json
